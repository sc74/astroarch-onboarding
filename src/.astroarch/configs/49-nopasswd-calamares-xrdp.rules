/* Allow Calamares to be started without password authentication XRDP
 */
polkit.addRule(function(action, subject)  {
   if (action.id == "org.freedesktop.policykit.exec" && action.lookup("program") == "/sbin/env")
       {
       return polkit.Result.YES;
   }
});

